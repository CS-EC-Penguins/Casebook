[tool.coverage.run]
source = ["."]
omit = [
    "evaluation/*",
    "__pycache__/*",
    "tests/*",
    "test_*.py",
    "*_test.py",
]

[tool.coverage.report]
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
]