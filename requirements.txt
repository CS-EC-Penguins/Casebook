# Verified 2026-06-30: these versions install and import together.
# langchain is held on the 0.3 line because ragas 0.4.3 imports
# langchain_community.chat_models.vertexai, which langchain 1.x removed.
# Revisit a langchain 1.x bump once ragas supports it (see fix plan task 7).
langchain==0.3.30
langchain-core==0.3.86
langchain-community==0.3.31
langchain-text-splitters==0.3.11
langchain-google-vertexai==2.1.2
langchain-postgres==0.0.17
psycopg[binary]==3.3.4
pgvector==0.3.6
ragas==0.4.3
datasets==5.0.0
langfuse==4.12.0
numpy
google-genai==1.75.0
tavily-python==0.7.26
mcp==1.28.1
langgraph==0.6.11
langchain-mcp-adapters==0.1.14
coverage==7.6.9

# Combined dependencies for all Week 11 labs. Install what each day's
# README asks for, or install everything here up front.
#
# Pinned to the Week 10 verified set. ragas 0.4.3 holds langchain on 0.3.x,
# so langgraph stays <1.0. pydantic v2 is required by with_structured_output.
google-cloud-aiplatform==1.159.0
tenacity==9.1.2
pydantic==2.12.3
pytest==9.1.1

